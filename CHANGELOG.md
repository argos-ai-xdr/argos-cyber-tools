# Changelog — argos-cyber-tools

Formato basado en [Keep a Changelog](https://keepachangelog.com/), SemVer. Actualizar al finalizar cada sprint.

## [Unreleased]

### Added
- `tool_catalog` (schema + definiciones firmables), `mcp_gateway` (autorización real), `policies` (OPA + aprobación con TTL/anti-replay/segregación), `executors` + `rollback` (idempotencia y reversión reales), `mcp_servers` (interfaz de solo lectura), `sandbox` (perfiles seccomp/AppArmor), `shuffle` (playbook de referencia).
