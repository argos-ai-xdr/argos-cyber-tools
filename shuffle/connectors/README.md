# connectors/

Conectores Shuffle hacia `mcp_gateway` y `argos-smartops` (aprobación) — pendientes de una instancia Shuffle real desplegada (`argos-platform/platform/`, no listada todavía como servicio de plataforma; añadir cuando ARG-021 lo requiera). Sin contenido hasta entonces.
